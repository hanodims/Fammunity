import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
  FeedDev: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
});
